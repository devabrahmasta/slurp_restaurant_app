name: slurp_restaurant_app
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.10.4

dependencies:
  flutter:
    sdk: flutter
  flutter_local_notifications: ^20.1.0
  flutter_timezone: ^5.0.1
  google_fonts: ^8.0.1
  http: ^1.6.0
  provider: ^6.1.5+1
  salomon_bottom_bar: ^3.3.2
  shared_preferences: ^2.5.4
  sqflite: ^2.4.2
  timezone: ^0.10.1
  workmanager:
    git:
      url: https://github.com/fluttercommunity/flutter_workmanager.git
      path: workmanager
      ref: main

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  mocktail: ^1.0.4
  test: ^1.26.3
  integration_test:
   sdk: flutter

flutter:
  uses-material-design: true
